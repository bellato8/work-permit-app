# ======================================================================
# ไฟล์ตัวอย่าง Environment Variables สำหรับ Vite
# คัดลอกไปเป็น .env.local และแก้ค่าตามจริง
# หมายเหตุ: เฉพาะตัวแปรที่ขึ้นต้น VITE_ เท่านั้นที่ฝั่งเว็บอ่านได้
# ======================================================================

VITE_SITE_NAME="Work Permit App"

# Firebase Configuration
VITE_FIREBASE_API_KEY=AIzaSyDOp1ThCGPjdKqAgCXiY4qZqeFtcnNWa6Q
VITE_FIREBASE_AUTH_DOMAIN=work-permit-app-1e9f0.firebaseapp.com
VITE_FIREBASE_PROJECT_ID=work-permit-app-1e9f0
VITE_FIREBASE_STORAGE_BUCKET=work-permit-app-1e9f0.firebasestorage.app
VITE_FIREBASE_APP_ID=1:532874719008:web:ebf5d8fca58414ea747145
VITE_FIREBASE_MESSAGING_SENDER_ID=532874719008
VITE_FIREBASE_MEASUREMENT_ID=G-186H83JTMX

# Application Settings
VITE_APP_BASE_URL=http://localhost:5173
VITE_APPROVER_EMAIL="asm.sutthirak@gmail.com"

# App ID สำหรับ Firestore paths (ใช้สร้าง path: artifacts/{appId}/...)
# ต้องตรงกับ Firebase Project ID หรือใช้ค่าที่กำหนดเอง
VITE_APP_ID=work-permit-app-1e9f0

# Cloud Functions Region (ต้องตรงกับ FUNCTIONS_REGION ใน functions/.env)
# สำหรับ Emulator ใช้: us-central1 (default)
# สำหรับ Production ใช้: asia-southeast1 (สิงคโปร์ - ใกล้ไทย)
VITE_FUNCTIONS_REGION=us-central1

# ======================================================================
# Cloud Functions & API Endpoints
# ⚠️ สำคัญ: ห้าม hardcode URLs เหล่านี้ในโค้ด ต้องใช้ environment variables เท่านั้น
# ======================================================================

# Base URL สำหรับ Cloud Functions (ใช้แทน hardcoded URL)
# รูปแบบ: https://{REGION}-{PROJECT_ID}.cloudfunctions.net
# ตัวอย่าง Dev: https://us-central1-work-permit-app-dev.cloudfunctions.net
# ตัวอย่าง Prod: https://asia-southeast1-work-permit-app-1e9f0.cloudfunctions.net
VITE_FUNCTIONS_BASE=https://asia-southeast1-work-permit-app-1e9f0.cloudfunctions.net

# ======================================================================
# Admin API Endpoints (Cloud Run / Cloud Functions)
# ⚠️ จำเป็นสำหรับ Admin Portal - ห้ามใช้ fallback hardcoded
# ======================================================================

# API สำหรับดึงข้อมูล Request แบบ Admin (single request)
VITE_GET_REQUEST_ADMIN_URL=https://your-api.example.com/getRequestAdmin

# API สำหรับแสดงรายการ Requests ทั้งหมด
VITE_LIST_REQUESTS_URL=https://your-api.example.com/listRequests

# API สำหรับแสดง Logs (auth logs, audit logs)
VITE_LIST_LOGS_URL=https://your-api.example.com/listLogs

# API สำหรับอัปเดตสถานะ Request (approve/reject)
VITE_UPDATE_STATUS_URL=https://your-api.example.com/updateStatus

# URL สำหรับ Decision Portal (หน้าอนุมัติสำหรับ approvers)
VITE_DECISION_PORTAL_URL=https://decision.example.com

# ======================================================================
# Admin Management APIs
# ⚠️ จำเป็นสำหรับจัดการผู้ดูแลระบบ
# ======================================================================

# API สำหรับแสดงรายการ Admins
VITE_LIST_ADMINS_URL=https://your-api.example.com/listAdmins

# API สำหรับเพิ่ม Admin ใหม่
VITE_ADD_ADMIN_URL=https://your-api.example.com/addAdmin

# API สำหรับเปลี่ยนบทบาท Admin (role change)
VITE_UPDATE_ADMIN_ROLE_URL=https://your-api.example.com/updateAdminRole

# API สำหรับลบ Admin
VITE_REMOVE_ADMIN_URL=https://your-api.example.com/removeAdmin

# API สำหรับเชิญ Admin (send invitation email)
VITE_INVITE_ADMIN_URL=https://your-api.example.com/inviteAdmin

# ======================================================================
# Authentication & Authorization
# ======================================================================

# API Key สำหรับ Approver authentication (ใช้แทน hardcoded "apl-2025")
# ⚠️ ห้าม commit ค่าจริงลง Git - ใช้ secrets management
VITE_APPROVER_KEY=your-api-key-here

# API สำหรับบันทึก Auth logs (login/logout)
VITE_LOG_AUTH_URL=https://your-api.example.com/logAuth

# ======================================================================
# Daily Operations APIs (Check-in/Check-out)
# ======================================================================

# API สำหรับ Check-in (บันทึกเข้างาน)
VITE_CHECKIN_URL=https://your-api.example.com/checkIn

# API สำหรับ Check-out (บันทึกออกงาน)
VITE_CHECKOUT_URL=https://your-api.example.com/checkOut

# API สำหรับดึงข้อมูลการทำงานรายวัน
VITE_GET_DAILY_WORK_BY_DATE_URL=https://your-api.example.com/getDailyWork

# API สำหรับดึงข้อมูล Calendar view
VITE_GET_CALENDAR_VIEW_URL=https://your-api.example.com/getCalendar

# ======================================================================
# Public APIs (ไม่ต้อง authentication)
# ======================================================================

# API สำหรับตรวจสอบสถานะ Permit (public endpoint)
VITE_GET_STATUS_URL=https://your-api.example.com/getStatus

# ======================================================================
# Company Information
# ======================================================================

# Website URL ของบริษัท (ใช้ใน PDF, emails, ฯลฯ)
# ⚠️ ห้าม hardcode ใน permitPdf.ts
VITE_COMPANY_WEBSITE_URL=https://example.com
