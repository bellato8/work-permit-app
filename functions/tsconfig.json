// ======================================================================
// File: functions/tsconfig.json
// เวอร์ชัน: 25/09/2025  — โหมด Node.js (ไม่มี DOM) + ชี้ type ของ Express ให้ชัด
// หน้าที่: ตั้งค่า TypeScript สำหรับ Cloud Functions v2 ให้ใช้ Express types
// หมายเหตุ:
//  - ตัด lib DOM ออก เพื่อไม่ให้ชนกับชื่อ Request/Response ของ Fetch API
//  - บังคับ "types": ["node", "express"] เพื่อให้ res.status()/req.query ใช้งานได้
//  - รวมไฟล์ประกาศเสริม (.d.ts) ใต้ src/types/* เข้ามาเสมอ
// ======================================================================
{
  "compilerOptions": {
    "target": "ES2021",
    "module": "commonjs",
    "lib": ["ES2021"],
    "moduleResolution": "node",
    "rootDir": "src",
    "outDir": "lib",
    "strict": true,
    "esModuleInterop": true,
    "forceConsistentCasingInFileNames": true,
    "types": ["node", "express"],
    "typeRoots": ["./node_modules/@types", "./src/types"],
    "skipLibCheck": true,
    "resolveJsonModule": true
  },
  "include": ["src/**/*.ts", "src/**/*.d.ts"],
  "exclude": ["node_modules", "lib"]
}
