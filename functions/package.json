{
  "name": "functions",
  "main": "lib/index.js",
  "engines": {
    "node": "20"
  },
  "private": true,
  "scripts": {
    "build": "tsc",
    "serve": "npm run build && npx firebase-tools@latest emulators:start --only functions",
    "deploy": "npm run build && npx firebase-tools@latest deploy --only functions",
    "deploy:one": "npm run build && npx firebase-tools@latest deploy --only",
    "deploy:getRequestAdmin": "npm run build && npx firebase-tools@latest deploy --only functions:getRequestAdmin",
    "login": "npx firebase-tools@latest login",
    "projects:list": "npx firebase-tools@latest projects:list",
    "lint": "echo \"(optional) add eslint here\""
  },
  "dependencies": {
    "@google-cloud/secret-manager": "^6.1.0",
    "@google-cloud/storage": "^7.17.0",
    "cors": "^2.8.5",
    "firebase-admin": "^13.5.0",
    "firebase-functions": "^6.4.0",
    "nodemailer": "^7.0.6",
    "qrcode": "^1.5.4",
    "recharts": "^3.2.1"
  },
  "devDependencies": {
    "@types/cors": "^2.8.19",
    "@types/express": "^4.17.23",
    "@types/node": "^20.19.11",
    "@types/nodemailer": "^6.4.19",
    "@types/qrcode": "^1.5.5",
    "tsx": "^4.20.6",
    "typescript": "^5.9.2"
  }
}
