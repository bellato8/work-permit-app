# ======================================================================
# functions/.env.example — Environment Variables สำหรับ Cloud Functions
# คัดลอกเป็น .env และแก้ค่าตามจริง
# หมายเหตุ: ไฟล์ .env จะไม่ถูก commit (อยู่ใน .gitignore)
# ======================================================================

# Cloud Functions Region (ใช้สำหรับ v2 API)
# ค่าที่แนะนำ: asia-southeast1 (สิงคโปร์), us-central1 (default)
FUNCTIONS_REGION=asia-southeast1

# Base URL สำหรับสร้างลิงก์ Contractor Form (Module 3)
# Production: https://imperialworld.asia/contractor/form
# Staging: https://work-permit-app-1e9f0.web.app/contractor/form
# Emulator/Local: http://localhost:5173/contractor/form
CONTRACTOR_FORM_BASE_URL=http://localhost:5173/contractor/form

# (สำหรับ emulator เท่านั้น - ไม่ต้องตั้งใน production)
# FIRESTORE_EMULATOR_HOST=localhost:8080
# FIREBASE_AUTH_EMULATOR_HOST=localhost:9099
